# Laravel test task

## Running

```bash
$ composer install
$ npm install
$ cp .env.example .env
$ npm run prod
$ php artisan key:generate
$ php artisan config:clear
$ php artisan serve
```# meeting_room_lock
